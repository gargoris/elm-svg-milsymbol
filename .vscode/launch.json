{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "runtimeExecutable": "/usr/bin/chromium",
            "name": "LocalHost:3000",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceRoot}"
        }
    ]
}